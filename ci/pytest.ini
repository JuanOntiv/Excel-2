[pytest]
# La suite vive en ci/ (este directorio). conftest.py agrega backend/ al
# sys.path, por lo que `import app...` funciona sin instalar el paquete.
testpaths = .
python_files = test_*.py
python_functions = test_*
addopts = -ra -q
